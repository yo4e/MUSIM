{
  "name": "musim",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "typecheck": "tsc -b --pretty false",
    "test": "vitest run",
    "experiment:cpu": "tsx scripts/cpu-sweep.ts",
    "experiment:damage": "tsx scripts/cpu-perturbation.ts"
  },
  "devDependencies": {
    "tsx": "^4.23.13",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
